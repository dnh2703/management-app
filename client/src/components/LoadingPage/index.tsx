import { Spinner } from '..'

const LoadingPage = () => {
  return (
    <div className='flex items-center justify-center w-screen h-screen'>
      <Spinner size='12' />
    </div>
  )
}

export default LoadingPage
